using System.Collections;
using System.Collections.Generic;
using UnityEngine.AI;
using UnityEngine;

public class enemyOnFoot : MonoBehaviour {


	// Use this for initialization
	void Start () {
	/*	coverDest = (Random.Range (0, 5));
		coverSpots = GameObject.FindGameObjectsWithTag("coverTrigger");
		player = GameObject.Find ("FPSController").transform;
		enemyAgent = this.gameObject.GetComponent<NavMeshAgent> ();
		enemyAgent.destination = coverSpots[coverDest].transform.position;
		StartCoroutine (setDestination ());*/
	}
	
	// Update is called once per frame
	void Update () {
	}


}
